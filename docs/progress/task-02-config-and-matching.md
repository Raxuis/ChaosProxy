# Task 02 — YAML configuration and route matching

- Status: `pending`
- Approval: not requested
- Depends on: task 01

## Goal

Add typed YAML configuration, aggregate validation, and precompiled first-match route rules.

## Checklist

- [ ] Implement `internal/config` types, duration decoding, loading, and validation.
- [ ] Report all validation errors with source lines where possible.
- [ ] Implement optional-method matching with `*` and `**` path semantics.
- [ ] Ignore query strings and define method case/trailing-slash behavior in tests.
- [ ] Add only `gopkg.in/yaml.v3`.
- [ ] Run matcher and validation tests plus `go test ./...`.
- [ ] Receive explicit user approval.

## Acceptance evidence

Not started.
